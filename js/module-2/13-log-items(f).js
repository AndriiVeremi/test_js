// Напиши функцію logItems(items) для перебора і логування масива

const logItems = function (items) {
    for (const item of items) {
        console.log(item);
    }
}

logItems(['mango', 'kiwi', 'poly', 'ajax']);
logItems([1, 2, 3, 4, 5]);
logItems(['aaaa', 'bbbb', 'cccc', 'dddd']);